services:
  jobs-worker:
    build:
      context: ../../
      dockerfile: services/jobs/Dockerfile
    container_name: home-telemetry-jobs-worker
    env_file:
      - .env
    command: dramatiq app.worker -p 2 -t 4
    deploy:
      replicas: 1
    volumes:
      - ./app:/app/app
    healthcheck:
      test: ["CMD", "python", "-c", "import redis; r = redis.from_url('${REDIS_URL:-redis://redis:6379/0}'); r.ping()"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - home-telemetry
    restart: unless-stopped

networks:
  home-telemetry:
    external: true
